(setq exordium-spell-check nil
      exordium-fci-mode :ondemand
      exordium-enable-org-export nil)
(add-hook 'c++-mode-hook 'fci-mode)

(setq exordium-display-powerline-after-idle-time 2)

(setq exordium-delete-trailing-whitespace t)

(setq exordium-enable-electric-pair-mode t)

(setq exordium-helm-everywhere t)

(setq exordium-rtags-cmake t)

(setq exordium-enable-insert-gui-primary-selection t)

(setq exordium-display-line-numbers nil)
(setq exordium-rtags-auto-complete t)

(setq exordium-powerline-theme :wave)
(setq exordium-highlight-linum nil)
